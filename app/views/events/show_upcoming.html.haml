- @page_title = h(@event.name)

%h1
  =h @event.name
  (Upcoming)

- if logged_in?
  - if @event.not.full? && current_user.not.rsvped?(@event)
    - form_tag event_rsvps_path(@event) do |form|
      = submit_tag 'RSVP!'
