<% @page_title = h(@event.name) -%>

<h1><%=h @event.name %> - Upcoming</h1>

<% unless current_user.rsvped?(@event) %>
 <p>
  <% form_tag rsvp_path(:event => @event.tag) do |form| %>
   <%= submit_tag 'RSVP!' %>
  <% end %>
 </p>
<% end %>
