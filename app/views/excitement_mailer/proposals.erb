Someone has submitted a proposal for <%= @event.name %>!

In order to keep the suspense up, you'll have to scroll down to see it.







































OK, you're in to this. Here's the details of the proposal that <%= @proposal.user.full_name %> (<%= @proposal.user.email %>) submitted.

Title: <%= @proposal.title %>

Proposal:
<%= @proposal.description %>

I hope you're as excited as I am.

You're happy, busy Webjam site.